spring.application.name=CONTENT-SERVICE
server.port=8081

# =========================
# DATABASE (DOCKER FIX)
# =========================
spring.datasource.url=jdbc:mysql://mysql-content:3306/content_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Hibernate (évite erreur dialect en Docker)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =========================
# EUREKA (si tu es en microservices)
# =========================
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
eureka.instance.prefer-ip-address=true

# =========================
# EMAIL (Gmail)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=asmaibrahim1920@gmail.com
spring.mail.password=ljzyzbkezopqbofy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true