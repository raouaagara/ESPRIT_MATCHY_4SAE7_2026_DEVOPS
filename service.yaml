apiVersion: v1
kind: Service
metadata:
  name: subscription-service
spec:
  type: NodePort
  selector:
    app: subscription-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080